conky.config = {
  out_to_x = true,
  own_window = true,
  own_window_type = 'dock',
  own_window_class = 'Conky',
  own_window_title = 'hxm-conky-test',
  own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
  double_buffer = true,
  minimum_width = 200,
  minimum_height = 60,
  alignment = 'top_left',
  gap_x = 10,
  gap_y = 10,
  update_interval = 1.0,
  use_xft = true,
  font = 'monospace:size=10',
  draw_shades = false,
  draw_outline = false,
  draw_borders = false,
}

conky.text = [[
hxm-conky-test
${time %H:%M:%S}
]]
